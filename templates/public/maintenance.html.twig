<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maintenance en cours — NTS Betting</title>
    <link rel="stylesheet" href="{{ asset('styles/variables.css') }}">
    <style>
        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--bg-panel);
            color: #fff;
            font-family: var(--font-body, sans-serif);
            text-align: center;
            padding: 24px;
        }
        .maintenance-box { max-width: 480px; }
        .maintenance-box i { font-size: 2.4rem; color: var(--color-gold, #DDB219); margin-bottom: 18px; }
        h1 { margin: 0 0 12px; }
        p { color: rgba(255,255,255,0.6); }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
</head>
<body>
    <div class="maintenance-box">
        <i class="fa-solid fa-screwdriver-wrench"></i>
        <h1>Maintenance en cours</h1>
        <p>{{ message ?: "Le site est en cours de maintenance. Merci de repasser dans quelques instants." }}</p>
    </div>
</body>
</html>
